export const PORT = process.env.PORT || 3000;
/* Using process.env.PORT for flexibility purposes, it ensures that the application can run on different environments without
changes to the code. Environment variables are often used for various deployment and production environments which can be
set by the OS, User, or the Cloud environemnt (hosting platform) */
